{% extends 'base.html.twig' %}

{% block title %}Site Index!{% endblock %}

{% block body %}
    {% for id, post in posts %}
        <p><a href="{{ path('site_show', {'id': id}) }}">ID {{ id }}</a></p>
        <h3>{{ post.title }}</h3>
        {#<p>{{ post.text }}</p>#}
    {% endfor %}
{% endblock %}
